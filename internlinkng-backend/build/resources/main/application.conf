ktor {
    deployment {
        port = 8080
        watch = [ com.internlinkng.backend ]
    }
    application {
        modules = [ com.internlinkng.backend.ApplicationKt.main ]
    }
}

database {
    url = "jdbc:postgresql://localhost:5432/internlinkng"
    driver = org.postgresql.Driver
    user = "postgres"
    password = "Android_Studio1"
} 